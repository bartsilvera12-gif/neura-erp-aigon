import PipelineVentasClient from "@/desktop/pages/PipelineVentasDesktop";

/**
 * Panel admin: pipeline de ventas de todos los vendedores.
 * Mismo componente para mobile/desktop (es una tabla admin, no algo touch-first).
 */
export default function Page() {
  return <PipelineVentasClient />;
}
